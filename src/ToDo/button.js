import React from "react";
const button = require('./style.css')

export default function Button() {
    return (
        <React.Fragment>
        <a href="#" style={button}>More</a>
        </React.Fragment>
    )
}